#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: the input number is an integer
 * @b: the input number is an integer
 * Return: The sum of a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
