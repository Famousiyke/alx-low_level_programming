#include <stdio.h>
/**
 * main - A c program that prints a line using the printf function
 * followed by a new line
 *
 * Return: Always 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
